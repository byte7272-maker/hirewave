# Where the FastAPI backend runs. The Next.js dev/prod server proxies
# /api/* and /health to this target (see next.config.mjs), so the browser
# always talks to the Next origin and never needs CORS.
API_PROXY_TARGET=http://127.0.0.1:8000
